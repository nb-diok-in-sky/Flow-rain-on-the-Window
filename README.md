# Flow-rain-on-the-Window
一个透过窗户看见水滴的小shader，考虑到这个前期的动画环节纯数学运算比较多，所以单独开了一个项目


项目整体要实现一个玻璃上面有着雨滴滑落的效果
将其分解不同的板块


# 雨滴滑落动画

雨滴滑落动画的实现需要通过物体对x、y轴进行随机偏移而得出来
基于这个问题 我们可以拆解出来
1 需要分x 和y方向进行不同的算法偏移
2 需要随机的参数来进行偏移
3隐藏的条件是 一般的雨滴可能存在最左边滑落到最右边这种夸张的效果 ，并且出现一定左右滑动的随机性，但在动画初期不选择考虑，
所以隐性限制是要将雨滴左右移动的范围控制在一个合适的范围内， 同时要保证一条雨滴从头滑倒尾的情况

自此，我们大概的分析清楚了需求，可以开始构建了

首先从雨滴开始建模，我们使用一个圆形作为雨滴的初步模型，先看图 这个图里面东西还是蛮多的
<img width="2508" height="1053" alt="Flow_Rain" src="https://github.com/user-attachments/assets/aac4423b-b20e-41e7-912c-609719abf95b" />
substruct里面默认减的是0.5,xy都是0.5
我们在这一阶段需要制作的东西是分割UV贴图 使得他的图片显示变成很多很多的小块，考虑到用来装载雨滴的，所以我们要使用竖着的长条形
使用二维向量和UV进行相乘，这里用的是(2，1) 让uv在x方向有更高的上限，并且把他拿去乘以一个变量，称作Size 之后可以用这个参数进行雨滴个数的调整 不过这都是后话
同时，为了让这些UV自上而下进行移动 我们取这个y分量与时间相乘，达到一个自上而下运动的效果
咱们把拆分后和时间相关联的变量进行合并后，拿去求小数部分，这样就会形成一个个的从0-1的网格部分
同时我们在这里创建两个随机数以后备用
把他拿去减0.5 这个就是我们的基础雨滴位置控制部分，这个数据是计算好的，为了之后的圆形雨滴放在这里做准备，本来是需要一步步经过后期调整，这里直接给出
刚刚那个数据拿去把刚刚的(2,1)向量除掉（这是因为绘制一个正常的圆形需要在一个正常的uv上绘制，所以将调整归0），然后使用length获得一个圆 再使用Smoothstep来进行圆形边界的处理，这样我们就可以得到圆形了


以正常的0-1的uv举例子
<img width="1805" height="747" alt="Circle_Draw" src="https://github.com/user-attachments/assets/f7d6c360-03f4-4ad5-b083-f88e098c895d" />
你对着这个普通的uv使用length，可以获得一个从左到上黑到白的一个扇形模糊渐变，咱们使用SmoothStem就可以得到一个较为清晰的边界
原理在于，length可以计算当前uv上这个坐标的uv值 得出他们与0，0点的距离，靠近0的就会标为黑or灰色，越远离0.0的就会越白，基于以上，使用smoothstep方法来选定你想要的边界，将其他的数值排成1
同时你发现，单纯的这样只能得到一个九十度开角的扇形 我们还需要把他挪到屏幕中心 
这就需要我们对uv坐标进行偏移 ，把uv坐标拿去加一某一个值，就可以达成把那个圆拖出来饿效果
原理依旧是length 他始终都是对着那个0.0点求距离，咱们用变量把他他偏移了，他和其他坐标的相对关系还是没变，所以距离也没变，自然圆就被挪动了
好了这只是个基础，具体的这个圆拿去干嘛之后再说

# 调试红线
这里插入一个调试雨滴的小妙招，在之后还蛮好用的，使用它可以很明显的发现我们雨滴的异常
<img width="1911" height="1017" alt="Red_Line" src="https://github.com/user-attachments/assets/2953e401-8f2c-44b5-a3ea-c0a1734eb17f" />
就是使用if语句来进行·`  
咱们拿到刚刚的那个uv分量，然后接入if模块，在A>B板块接入一个自己喜欢的颜色，这样当满足条件的时候可以生成一个框框，这里的B我填写的是0.465 
这里是因为到0.5的时候基本上就是目前我所设定的分割块的边界，这个可以按照自己喜好来，我按照的是0.5分一次，为什么是0.465这个数是因为再高了不行 不做调整画不出来 具体原因应该是根画线本身的强度有关  


# Y方向动画
ok这里来看y方向上的移动
也就是雨滴下落的动画表现，我们预期希望这个雨滴他偶尔慢偶尔快的一个运动表现，符合现实中客观存在的雨滴下流场景 时快时慢的感觉
<img width="2499" height="657" alt="Y_Move" src="https://github.com/user-attachments/assets/28babb59-8d27-46d7-9a47-5f273af57429" />
这里的知识点主要在于函数的运用，原理是使用好你原来V方向的分量减去你算好的函数结果
剩下的就是准备一个函数了
这里可以去函数可视化网站自由发挥 要达到的节奏你自己喜欢就行
这里选用的是这样的：
使用V分量-0.29 * sin(sin(0.5*sin（time.y + Random * FlowIntensity）+(time.y * Random * FlowIntensity))+time.y * Random * FlowIntensity)
数学，很奇妙吧
这里使用负数乘以这个最后结尾的sin函数是因为
咱们要的函数效果是下坠 使用正数的话会呈现一个上升的状态，咱们不要那个
原理解析的话咱们不看那个函数 函数自身变化比较多，属于自己随便搓出来的
来看减法的逻辑
咱们V分量获得的是一各个从下到上0到1的值 接近1代表这个地方是可见，允许你展现你的内容，接近0或者成为0会被抹除
然后咱们加了这么一大堆的东西，并且把他变成了负号 你减了一个负数 那就是加了某种东西
以此为基础，咱们来看，首先那个随机数 是不断向下流动 并且在不断地变化中，我们原本平平的V分量会加上这一部分sin值 然后关键来了，这一个V分量平平从黑到白的这个区域在0-1的范围内
然后他这个一块区域会整体加上刚刚的那一大长串求出的sin值  然后就会因为点位上具体值的大小不同，出现白色的区域放大(都加了某个值）or缩小(都减了某个值) 然后因为那个sin函数给的比较神人，所以每个部位减去的东西也不同
然后它这一整个随机数又是通过floor方法来保持一整块区域都是这一个值的  这个Y方向的动画就是这样
一句话总结就是 所有相同的区块，减去了随机生成的数值，所以它所形成的图形就会该改变 

# X方向






 










